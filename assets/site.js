(()=>{'use strict';document.documentElement.classList.add('js');const button=document.querySelector('.menu-toggle');const nav=document.querySelector('#nav');const close=()=>{button?.setAttribute('aria-expanded','false');button?.setAttribute('aria-label','Ouvrir le menu');nav?.classList.remove('open')};button?.addEventListener('click',()=>{const open=button.getAttribute('aria-expanded')!=='true';button.setAttribute('aria-expanded',String(open));button.setAttribute('aria-label',open?'Fermer le menu':'Ouvrir le menu');nav.classList.toggle('open',open)});document.addEventListener('keydown',event=>{if(event.key==='Escape'&&nav?.classList.contains('open')){close();button.focus()}});document.addEventListener('click',event=>{if(!event.target.closest('.header'))close()});window.matchMedia('(min-width:821px)').addEventListener('change',close);const reveal=document.querySelectorAll('.reveal');if('IntersectionObserver'in window&&!window.matchMedia('(prefers-reduced-motion: reduce)').matches){const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('visible');entry.target.classList.remove('pending');observer.unobserve(entry.target)}})},{threshold:.07});reveal.forEach(item=>{item.classList.add('pending');observer.observe(item)})}})();
(()=>{const header=document.querySelector('.header');let scheduled=false;const update=()=>{header?.classList.toggle('scrolled',window.scrollY>12);scheduled=false};window.addEventListener('scroll',()=>{if(!scheduled){scheduled=true;requestAnimationFrame(update)}},{passive:true});update();document.querySelectorAll('.service-grid,.steps-grid').forEach(group=>{[...group.children].forEach((item,i)=>item.style.setProperty('--reveal-delay',`${i*90}ms`))})})();
